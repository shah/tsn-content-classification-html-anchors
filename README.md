# Content Classification for HTML Anchors

Classify HTML anchors (links) as content or non-content

# Dependencies

* NodeJS 14+ with TypeScript 3.9+

# One-time Setup

    npm login --registry=https://npm.pkg.github.com
    npm install

